{
  "name": "ansi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "format": "prettier --single-quote --trailing-comma all --write src",
    "compile": "yarn tsc",
    "make-runnable": "chmod +x dist/index.js",
    "build": "yarn format && yarn compile && yarn make-runnable"
  },
  "devDependencies": {
    "@types/node": "^14.14.25",
    "prettier": "^2.2.1",
    "typescript": "^4.1.3"
  }
}
